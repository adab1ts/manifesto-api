{
  "name": "manifest-api",
  "version": "1.0.0",
  "description": "Manifest's API",
  "main": "server/server.js",
  "engines": {
    "node": ">=4"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/adabits/manifest-api.git"
  },
  "license": "MIT",
  "scripts": {
    "lint": "eslint .",
    "start": "node .",
    "dev": "nodemon server/server.js --watch server",
    "posttest": "npm run lint && nsp check"
  },
  "dependencies": {
    "compression": "^1.0.3",
    "cors": "^2.5.2",
    "helmet": "^1.3.0",
    "loopback": "^3.0.0",
    "loopback-component-explorer": "^5.0.0",
    "loopback-boot": "^2.6.5",
    "serve-favicon": "^2.0.1",
    "strong-error-handler": "^2.0.0"
  },
  "devDependencies": {
    "dotenv": "^4.0.0",
    "eslint": "^3.17.1",
    "eslint-config-loopback": "^8.0.0",
    "nodemon": "^1.12.1",
    "nsp": "^2.1.0"
  }
}
